import java.util.TreeSet;

public class LearnTreeSet {

    public static void main(String[] args){
        TreeSet<Integer> tr = new TreeSet<>();

        //its same as hashset
        //Same properities
        //No duplication of elements

        //Just one difference that it helps to create binary search tree
        //thats why elements are always stored in sorted way no matter how they are being added


        //one more thing
        //Hash set - O(1)
        //tree set - o(logn)
    }
}
